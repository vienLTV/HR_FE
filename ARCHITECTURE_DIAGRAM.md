# Employee Creation Feature - Visual Architecture

## 🏗️ Component Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    EmployeePage                         │
│  (src/app/(dashboard)/employee/page.tsx)               │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │  Header                                        │    │
│  │  ┌──────────────┐  ┌──────────────────────┐   │    │
│  │  │    Title     │  │ CreateEmployeeButton │   │    │
│  │  │  "Employee"  │  │   (OWNER only)       │   │    │
│  │  └──────────────┘  └──────────────────────┘   │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │            DataTable (Employee List)           │    │
│  │  refreshKey={refreshKey}                       │    │
│  └────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
                        │
                        │ onClick
                        ↓
┌─────────────────────────────────────────────────────────┐
│              CreateEmployeeModal                         │
│  (src/components/employee/CreateEmployeeModal.tsx)     │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │  Modal Header                                  │    │
│  │  [X] Create Employee Account                   │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │  Form                                          │    │
│  │  ┌──────────────────────────────────────┐     │    │
│  │  │ Full Name *                          │     │    │
│  │  │ [__________________________]         │     │    │
│  │  └──────────────────────────────────────┘     │    │
│  │  ┌──────────────────────────────────────┐     │    │
│  │  │ Email *                              │     │    │
│  │  │ [__________________________]         │     │    │
│  │  └──────────────────────────────────────┘     │    │
│  │  ┌──────────────────────────────────────┐     │    │
│  │  │ Password *                           │     │    │
│  │  │ [__________________________]         │     │    │
│  │  └──────────────────────────────────────┘     │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │  [Cancel]  [Create Employee]                   │    │
│  └────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
                        │
                        │ onSubmit
                        ↓
┌─────────────────────────────────────────────────────────┐
│                createUser() API                          │
│           (src/lib/api/users.ts)                        │
│                                                          │
│  1. Get JWT from localStorage                           │
│  2. POST /users with request body                       │
│  3. Handle 401 → redirect to login                      │
│  4. Parse response                                       │
│  5. Return CreateUserResponse or throw error            │
└─────────────────────────────────────────────────────────┘
                        │
                        │ HTTP Request
                        ↓
┌─────────────────────────────────────────────────────────┐
│              Backend API Server                          │
│              (Quarkus/Java)                             │
│                                                          │
│  POST /users                                            │
│  ┌────────────────────────────────────────────────┐    │
│  │  1. Verify JWT signature                       │    │
│  │  2. Check OWNER role                           │    │
│  │  3. Validate input data                        │    │
│  │  4. Check email uniqueness                     │    │
│  │  5. Hash password                              │    │
│  │  6. Create user in database                    │    │
│  │  7. Return 201 Created                         │    │
│  └────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow Diagram

```
┌──────────┐
│  OWNER   │ (logged in)
│   User   │
└─────┬────┘
      │
      │ 1. Sees "Create Employee" button
      │    (localStorage.role includes "OWNER")
      ↓
┌─────────────────┐
│ Button Visible  │
└─────┬───────────┘
      │
      │ 2. Clicks button
      ↓
┌─────────────────┐
│  Modal Opens    │
└─────┬───────────┘
      │
      │ 3. Fills form:
      │    - fullName: "John Doe"
      │    - email: "john@example.com"
      │    - password: "password123"
      │    - role: "USER" (hidden)
      ↓
┌─────────────────┐
│ Form Validation │
│  (Client-side)  │
└─────┬───────────┘
      │
      │ 4. Valid? → Continue
      │    Invalid? → Show errors
      ↓
┌─────────────────┐
│  API Call       │
│  createUser()   │
└─────┬───────────┘
      │
      │ 5. Add headers:
      │    Authorization: Bearer <JWT>
      │    Content-Type: application/json
      ↓
┌─────────────────┐
│  POST /users    │
└─────┬───────────┘
      │
      │ 6. Backend processes
      ↓
┌─────────────────┐
│   Response      │
│ ┌─────────────┐ │
│ │ Success?    │ │
│ └─────┬───┬───┘ │
│       │   │     │
│   Yes │   │ No  │
│       ↓   ↓     │
│   ┌───┐ ┌────┐ │
│   │201│ │4xx │ │
│   └───┘ └────┘ │
└─────┬───────┬───┘
      │       │
      │       │ 7. Show error message
      │       │    Stay in modal
      │       ↓
      │   ┌──────────────┐
      │   │ Error Banner │
      │   │ (Red)        │
      │   └──────────────┘
      │
      │ 8. Show success message
      ↓
┌──────────────────┐
│  Success Banner  │
│    (Green)       │
└─────┬────────────┘
      │
      │ 9. Wait 1.5s
      ↓
┌──────────────────┐
│  onSuccess()     │
│  callback        │
└─────┬────────────┘
      │
      │ 10. Refresh employee list
      │     setRefreshKey(prev => prev + 1)
      ↓
┌──────────────────┐
│  DataTable       │
│  Re-fetches data │
└─────┬────────────┘
      │
      │ 11. Modal closes
      ↓
┌──────────────────┐
│  Back to         │
│  Employee Page   │
│  (with new row)  │
└──────────────────┘
```

## 📂 File Structure Tree

```
cetus-ui-master/
│
├── 📄 EMPLOYEE_CREATION_README.md      ← Quick start guide
├── 📄 EMPLOYEE_CREATION_GUIDE.md       ← Full documentation
├── 📄 IMPLEMENTATION_SUMMARY.md        ← This summary
├── 📄 QUICK_REFERENCE.md               ← Cheat sheet
│
└── src/
    ├── app/
    │   ├── (dashboard)/
    │   │   └── employee/
    │   │       └── page.tsx            ← ✅ Updated (integrated button)
    │   │
    │   └── utils/
    │       └── api.js                  ← Existing auth helper
    │
    ├── components/
    │   └── employee/
    │       ├── CreateEmployeeButton.tsx  ← ✅ New (main component)
    │       ├── CreateEmployeeModal.tsx   ← ✅ New (modal + form)
    │       └── examples.tsx              ← ✅ New (usage examples)
    │
    └── lib/
        ├── api/
        │   └── users.ts                ← ✅ New (API client)
        │
        └── types/
            └── employee.ts             ← ✅ New (TypeScript types)
```

## 🎭 Role-Based Rendering

```
┌─────────────────────────────────────┐
│  User Authentication State          │
└──────────────┬──────────────────────┘
               │
      ┌────────┴────────┐
      │                 │
      ↓                 ↓
┌──────────┐      ┌──────────┐
│  OWNER   │      │   USER   │
│   Role   │      │   Role   │
└────┬─────┘      └────┬─────┘
     │                 │
     │                 │
     ↓                 ↓
┌──────────────┐  ┌──────────────┐
│ Button       │  │ Button       │
│ VISIBLE      │  │ HIDDEN       │
│              │  │              │
│ ┌──────────┐│  │ null         │
│ │ Create   ││  │              │
│ │ Employee ││  │              │
│ └──────────┘│  │              │
└──────────────┘  └──────────────┘
```

## 🔐 Security Flow

```
┌─────────────────────────────────────────────┐
│           Client-Side Security              │
├─────────────────────────────────────────────┤
│                                             │
│  1. Check Role from localStorage            │
│     if (!role.includes("OWNER"))            │
│        return null  // Hide button          │
│                                             │
│  2. Get JWT from localStorage               │
│     const token = localStorage.getItem()    │
│                                             │
│  3. Send with request                       │
│     Authorization: Bearer <token>           │
│                                             │
│  4. Handle 401 errors                       │
│     if (status === 401) {                   │
│       localStorage.removeItem("token")      │
│       redirect("/login")                    │
│     }                                       │
│                                             │
└─────────────────┬───────────────────────────┘
                  │
                  │ HTTP Request
                  ↓
┌─────────────────────────────────────────────┐
│           Server-Side Security              │
├─────────────────────────────────────────────┤
│                                             │
│  1. Verify JWT Signature                    │
│     SmallRye JWT validates RS256            │
│                                             │
│  2. Extract Claims                          │
│     groups = jwt.getGroups()                │
│                                             │
│  3. Check OWNER Role                        │
│     @RolesAllowed("OWNER")                  │
│     if (!groups.contains("OWNER"))          │
│        return 403 Forbidden                 │
│                                             │
│  4. Validate Input                          │
│     - Email format                          │
│     - Email uniqueness                      │
│     - Password requirements                 │
│                                             │
│  5. Sanitize Data                           │
│     - Remove HTML/scripts                   │
│     - Trim whitespace                       │
│                                             │
│  6. Hash Password                           │
│     BCrypt.hash(password)                   │
│                                             │
│  7. Create User                             │
│     Save to database                        │
│                                             │
│  8. Return Response                         │
│     201 Created or 4xx Error                │
│                                             │
└─────────────────────────────────────────────┘
```

## 🎨 UI States Diagram

```
┌────────────────────────────────────────────────┐
│              Button States                     │
├────────────────────────────────────────────────┤
│                                                │
│  DEFAULT (Idle)                                │
│  ┌──────────────────────────────────────┐     │
│  │  [+] Create Employee                 │     │
│  │  ↑ Blue bg, white text               │     │
│  └──────────────────────────────────────┘     │
│                                                │
│  HOVER                                         │
│  ┌──────────────────────────────────────┐     │
│  │  [+] Create Employee                 │     │
│  │  ↑ Darker blue bg                    │     │
│  └──────────────────────────────────────┘     │
│                                                │
│  FOCUS (Keyboard)                              │
│  ┌──────────────────────────────────────┐     │
│  │  [+] Create Employee                 │     │
│  │  ↑ Blue ring around                  │     │
│  └──────────────────────────────────────┘     │
│                                                │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│              Modal States                      │
├────────────────────────────────────────────────┤
│                                                │
│  IDLE (Form Empty)                             │
│  ┌──────────────────────────────────────┐     │
│  │  Full Name: [____________]           │     │
│  │  Email:     [____________]           │     │
│  │  Password:  [____________]           │     │
│  │                                      │     │
│  │  [Cancel]  [Create Employee]         │     │
│  └──────────────────────────────────────┘     │
│                                                │
│  LOADING (Submitting)                          │
│  ┌──────────────────────────────────────┐     │
│  │  Full Name: [John Doe________]       │     │
│  │  Email:     [john@ex_________]       │     │
│  │  Password:  [••••••••________]       │     │
│  │                                      │     │
│  │  [Cancel]  [⟳ Creating...]          │     │
│  │  ↑ Disabled  ↑ Spinner + text       │     │
│  └──────────────────────────────────────┘     │
│                                                │
│  SUCCESS                                       │
│  ┌──────────────────────────────────────┐     │
│  │  ✅ Employee created successfully!   │     │
│  │  ↑ Green banner                      │     │
│  │                                      │     │
│  │  Full Name: [John Doe________]       │     │
│  │  Email:     [john@ex_________]       │     │
│  │  Password:  [••••••••________]       │     │
│  │                                      │     │
│  │  [Cancel]  [Create Employee]         │     │
│  │  ↑ Enabled again                     │     │
│  └──────────────────────────────────────┘     │
│  (Auto-closes after 1.5s)                     │
│                                                │
│  ERROR                                         │
│  ┌──────────────────────────────────────┐     │
│  │  ❌ Email already exists              │     │
│  │  ↑ Red banner                        │     │
│  │                                      │     │
│  │  Full Name: [John Doe________]       │     │
│  │  Email:     [john@ex_________]       │     │
│  │  Password:  [••••••••________]       │     │
│  │                                      │     │
│  │  [Cancel]  [Create Employee]         │     │
│  │  ↑ Stays open for correction         │     │
│  └──────────────────────────────────────┘     │
│                                                │
└────────────────────────────────────────────────┘
```

## 🧩 Integration Points

```
┌──────────────────────────────────────────┐
│  Existing Codebase                       │
├──────────────────────────────────────────┤
│                                          │
│  1. localStorage (Auth)                  │
│     └─→ token, role                      │
│         Used by: CreateEmployeeButton    │
│                                          │
│  2. DataTable Component                  │
│     └─→ Employee list display            │
│         Refreshed by: key prop           │
│                                          │
│  3. API Base URL                         │
│     └─→ NEXT_PUBLIC_API_BASE_URL        │
│         Used by: src/lib/api/users.ts    │
│                                          │
│  4. Tailwind CSS                         │
│     └─→ Styling framework                │
│         Used by: All components          │
│                                          │
│  5. TypeScript Config                    │
│     └─→ Strict mode, path aliases        │
│         Used by: All .ts/.tsx files      │
│                                          │
└──────────────────────────────────────────┘
```

## 📊 Error Handling Matrix

```
┌──────────────┬──────────────┬─────────────────┬──────────────┐
│ Error Type   │ Status Code  │ Client Handling │ User Sees    │
├──────────────┼──────────────┼─────────────────┼──────────────┤
│ Validation   │ 400          │ Show error      │ Red banner   │
│ Error        │              │ banner          │ with message │
├──────────────┼──────────────┼─────────────────┼──────────────┤
│ Unauthorized │ 401          │ Clear token +   │ Login page   │
│              │              │ redirect        │              │
├──────────────┼──────────────┼─────────────────┼──────────────┤
│ Forbidden    │ 403          │ Show error      │ Red banner   │
│              │              │ banner          │ "No access"  │
├──────────────┼──────────────┼─────────────────┼──────────────┤
│ Duplicate    │ 409          │ Show error      │ Red banner   │
│ Email        │              │ banner          │ "Email used" │
├──────────────┼──────────────┼─────────────────┼──────────────┤
│ Server Error │ 500          │ Show error      │ Red banner   │
│              │              │ banner          │ "Try again"  │
├──────────────┼──────────────┼─────────────────┼──────────────┤
│ Network      │ N/A          │ Show error      │ Red banner   │
│ Error        │              │ banner          │ "No network" │
└──────────────┴──────────────┴─────────────────┴──────────────┘
```

---

**Visual architecture complete!** 🎨
Ready for implementation and testing. 🚀
